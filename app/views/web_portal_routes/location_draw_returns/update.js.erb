// Show flash messages
$(".flash_message").remove();
$(".content").prepend("<%= j(render 'shared/flash_messages') %>");

var errors = <%= raw errors %>;
var model = "location_draw_returns";
show_rails_validation_errors(model, errors);
