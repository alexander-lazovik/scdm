<% flash.each do |message_type, message| %>
  <div class="flash_message alert <%= bootstrap_class_for(message_type) %> alert-dismissible" role="alert">
    <button class="close" data-dismiss="alert"><span aria-hidden="true">×</span>
    <span class="sr-only">Close</span></button>
    <%= message %>
  </div>
<% end %>
